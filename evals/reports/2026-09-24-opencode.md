# Evaluation report — opencode

## 1. Environment

- date: 2026-09-24
- harness: opencode (1.18.32)
- model: (harness default)
- skill commit: 67aa797
- repeats per case: 1 × (`add-caching-auto`, `fully-specified-auto-quiet`, `no-repo`, `vague-no-ask`)
- contamination check: yes (reply: 'OK')
- skill visible in a prepared workspace: yes

`opencode.json` written into every workspace:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "skill": "allow",
    "bash": {
      "git log*": "allow",
      "git show*": "allow",
      "*": "deny"
    },
    "edit": "deny",
    "task": "deny",
    "question": "deny",
    "webfetch": "deny",
    "websearch": "deny",
    "external_directory": "deny",
    "doom_loop": "allow"
  }
}
```

## 2. Summary

| metric | value |
|---|---|
| cases | 4 |
| passed | 4 |
| probe ratio (mean, 3/4 spec-bearing turns) | 0.25 |
| over-asks | 0 |
| auto-trigger | fires when it should: 1/1 |
| degraded output | 0 |
| timeouts | 0 |
| harness errors | 0 |
| not triggered | 0 |
| evidence form violations | 0 |
| hallucinated evidence | 0 |
| would-pass-if-parseable | 0 |

Subset run — suite threshold not applicable; all 4 selected case(s) must pass. Result: **PASS**.

## 3. Per case

| case | states | passed | failed assertions |
|---|---|---|---|
| `add-caching-auto` | ASK | 1/1 | — |
| `fully-specified-auto-quiet` | - | 1/1 | — |
| `no-repo` | ASK | 1/1 | — |
| `vague-no-ask` | HALT | 1/1 | — |

## 4. Iterations

Release-verification run for v1.1.0 ("save every emitted spec to `.intent/` by default").
These four cases exercise the paths the change can disturb, one run each: `add-caching-auto`
asks (ASK emits a file too), `no-repo` has no sources to probe (ASK in an empty workspace),
`vague-no-ask` halts underspecified (HALT emits a file too), `fully-specified-auto-quiet`
must stay silent (silence still writes nothing). The suite workspaces run with `edit: deny`,
so opencode removes the writing tools from the toolset entirely ("Available tools: glob,
grep, invalid, read, skill, todowrite" in the transcripts) — these four cases test that
everything else is unchanged when there is nothing to write with. All 4 passed.

Delivery verification (skill arm, 1 run, separately snapshotted): session 1 wrote
`.intent/add_caching.intent.yaml` with a schema-valid ASK spec (`intent` = filename stem)
before emitting the fenced reply; session 2 rewrote the same path; the final snapshot file
is a schema-valid ROUTE; `meta.json` states read ASK, ROUTE, null across the three sessions
with session 1 status `ok`. All four release assertions (a1–a4) hold. The run scored 5/6
(item 5, `states_failure_policy`, not stated this time — recorded, not gated; the v1.0.0
skill arm went 3/3 at 6/6 and this single run is not a measurement of the writing change).

## 5. Line for the README

```
2026-09-24 · opencode · (harness default) · 4/4 subset cases · probe ratio 0.25 · 0 over-asks · 0 hallucinated evidence
```
