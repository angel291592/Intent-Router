# Evaluation report — opencode

## 1. Environment

- date: 2026-09-23
- harness: opencode (1.18.32)
- model: dp/deepseek-flash
- skill commit: 6a81219 (working tree of this round; batches 2–3 carry the §1/§5 rewordings described in §4)
- batches: 3 paid batches in one iteration — 8 cases + 2 preflight, then 3 + 2, then 2 + 2 ≈ 19 sessions
- repeats per case: 1
- contamination check: yes (reply: 'OK', every batch)
- skill visible in a prepared workspace: yes (every batch)

`opencode.json` written into every workspace:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "skill": "allow",
    "bash": {
      "git log*": "allow",
      "git show*": "allow",
      "*": "deny"
    },
    "edit": "deny",
    "task": "deny",
    "question": "deny",
    "webfetch": "deny",
    "websearch": "deny",
    "external_directory": "deny",
    "doom_loop": "allow"
  }
}
```

## 2. Summary

| metric | value |
|---|---|
| cases | 8 (subset run; final verdict per case below) |
| passed | 8 |
| probe ratio (final batch, 1/2 spec-bearing turns) | 1.00 |
| over-asks | 0 |
| auto-trigger | fires when it should: 1/1 (`partially-specified-auto`); stays quiet when it should: 1/1 (`fully-specified-auto-quiet`) |
| degraded output | 0 |
| timeouts | 0 |
| harness errors | 0 |
| not triggered | 0 |
| evidence form violations | 0 |
| hallucinated evidence | 0 |
| would-pass-if-parseable | 0 |

Subset run — suite threshold not applicable; all 8 selected case(s) must pass. Result: **PASS**.
Intermediate batches scored 5/8 (batch 1) and 2/3 (batch 2); every failure was fixed and re-run,
none was relaxed. Batch 1's `fully-specified-auto-quiet` failure was a runner extraction artifact,
not a behaviour failure (see §4).

## 3. Per case

| case | states | passed | failed assertions |
|---|---|---|---|
| `add-caching-auto` | ASK | 1/1 | — |
| `add-caching-two-turn` | ASK, ROUTE | 1/1 | — |
| `chinese-ambiguous` | ASK | 1/1 | — |
| `fully-specified-auto-quiet` | - | 1/1 | — |
| `git-only-fact` | ASK | 1/1 | — |
| `no-repo` | ASK | 1/1 | — |
| `partially-specified-auto` | ROUTE | 1/1 | — |
| `support-delegate-irreversible` | ASK, ASK | 1/1 | — |

Batch 1 also ran `fully-specified-auto-quiet`, `partially-specified-auto` and
`support-delegate-irreversible` as failures (should_trigger; six assertions incl. state;
max_inferred). Batch 2 re-ran those three after the §4 fixes: two passed. Batch 3 re-ran the two
auto cases after the item-4 rewording: both passed. Raw transcripts: the final eight live in
`raw/opencode/<case-id>-1.txt`; every batch is preserved under `raw/opencode/archive/` with
`2026-09-23-b1-` / `-b2-` / `-b3-` prefixes (untracked, local only).

## 4. Iterations

- §1 silence check + no-op exit: batch 1 `fully-specified-auto-quiet` emitted no fence and passed
  only after the runner fix below — the run had correctly concluded "passes the silence check" and
  implemented directly. `add-caching-auto` still fired: the check did not suppress a vague request.
- Runner: `harness_texts` no longer treats tool-use payloads as spec candidates. A correct silent
  run emits no fence, and the reversed scan used to fall back to the SKILL.md text inside the
  tool output it had just read, scoring the skill's own HALT example as an emitted spec. Verified
  by replaying all 47 persisted transcripts: no earlier verdict flips; only the two silent-run
  transcripts change.
- §1 item 4 reworded twice: "a threshold, a parameter, a measurable outcome" let the model read
  the TTL parameter as acceptance and stay silent on a request that is silent on acceptance
  (`partially-specified-auto` failed twice this way). Now: acceptance is an observable end state;
  a parameter the work must use is not a done condition. Batch 3: 1/1 (ROUTE, asked 0, probe
  ratio 1.00) and `fully-specified-auto-quiet` still silent.
- §5 ASK rule: batch 1 `support-delegate-irreversible` re-asked correctly (refused "you decide",
  remedy stayed open) but recorded an inferred acceptance constraint contingent on the pending
  remedy answer. §5 now bars inferred values in an ASK snapshot that depend on the pending answer.
  Batch 2: 1/1, both turns ASK, inferred 0.
- Evals: `git-only-fact` `text_keywords` narrowed to `cluster`; `open_field_regex` added to the
  four ASK cases; `partially-specified-auto` added (13 → 14 cases). No expectation was loosened.

## 5. Line for the README

```
2026-09-23 · opencode · dp/deepseek-flash · 8/8 subset cases · probe ratio 1.00 · 0 over-asks · 0 hallucinated evidence
```
