# Evaluation report — opencode

> Note: the 2026-09-23 Tier 4 run and its re-judgement replaced 9 of the 10 transcripts in
> `raw/opencode/` that backed the 2026-09-22 report. `raw/` is gitignored, so those transcripts
> are not recoverable; the 2026-09-22 report itself is committed and unchanged. `run_suite` now
> archives a transcript under `raw/<harness>/archive/` before overwriting it.

## 1. Environment

- date: 2026-09-23
- harness: opencode (1.18.32)
- model: dp/deepseek-flash
- skill commit: a8cc058
- repeats per case: 1 × (`add-caching-auto`, `add-caching-two-turn`, `chinese-ambiguous`, `fully-specified`, `fully-specified-auto-quiet`, `git-only-fact`, `no-repo`, `question-not-trigger`, `ungrillable`, `vague-no-ask`)
- contamination check: yes (reply: 'OK')
- skill visible in a prepared workspace: yes

`opencode.json` written into every workspace:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "skill": "allow",
    "bash": {
      "git log*": "allow",
      "git show*": "allow",
      "*": "deny"
    },
    "edit": "deny",
    "task": "deny",
    "question": "deny",
    "webfetch": "deny",
    "websearch": "deny",
    "external_directory": "deny",
    "doom_loop": "allow"
  }
}
```

## 2. Summary

| metric | value |
|---|---|
| cases | 10 |
| passed | 8 |
| probe ratio (mean, 9/11 spec-bearing turns) | 0.56 |
| over-asks | 0 |
| auto-trigger | fires when it should: 1/1; stays quiet when it should: 1/1 |
| degraded output | 0 |
| timeouts | 0 |
| harness errors | 0 |
| not triggered | 0 |
| evidence form violations | 0 |
| hallucinated evidence | 0 |
| would-pass-if-parseable | 0 |

Threshold: at least 8 of 10 cases pass **and** hallucinated evidence is 0. Result: **MET**.

## 3. Per case

| case | states | passed | failed assertions |
|---|---|---|---|
| `add-caching-auto` | ASK | 1/1 | — |
| `add-caching-two-turn` | ASK, ROUTE | 1/1 | — |
| `chinese-ambiguous` | ASK | 1/1 | — |
| `fully-specified` | ROUTE | 1/1 | — |
| `fully-specified-auto-quiet` | - | 0/1 | should_trigger |
| `git-only-fact` | ASK | 1/1 | — |
| `no-repo` | ASK | 1/1 | — |
| `question-not-trigger` | - | 1/1 | — |
| `ungrillable` | ROUTE | 0/1 | state_in |
| `vague-no-ask` | HALT | 1/1 | — |

## 4. Iterations

- S1 §6 quoting rule + dirty-scalar example: Tier 3 git-only-fact 2/2, fully-specified 2/2, ungrillable 2/2 — the new model's degraded_output failures are gone (Tier 4 degraded output 0).
- S2 §2 evidence definition as a single whitespace-free token: Tier 4 has zero evidence_valid and zero evidence_form.
- S3 §4.2 empty probe surface is not a failed lookup: Tier 3 no-repo 2/2; Tier 4 no-repo ASK.
- S4 §1 no-ask may infer how, never what: Tier 3 vague-no-ask 2/2.
- S4 scope correction: Tier 3 chinese-ambiguous halted in 1 of 2 runs because the no-ask halt rule leaked into asking mode. §1 now scopes that halt to no-ask mode and §5 reserves HALT underspecified for when asking is impossible. Re-run: 3/3.
- S5 §7 recognise aesthetic targets before spending probe budget: Tier 3 ungrillable 2/2.
- S6–S9 reserved `user:delegated` and the evidence contract: no transcript emitted the old delegation sentence.
- Runner R1–R11 plus a form-rule fix: a legitimate dotfile pointer (`.github/workflows/ci.yml`) had been misjudged as evidence_form because `.` was treated as a prefix. Fixed; Tier 0 47/47.

Two failures remain in this run, both about case expectations rather than plumbing:
- `fully-specified-auto-quiet` (should_trigger): the model still runs in auto mode on a fully
  specified request. Decision 1 accepted this may fail first.
- `ungrillable` (state_in): the model ROUTEd with `target: prototype`, which the case's
  `state_in: [ASK, HALT]` rejects — but SKILL.md §7 explicitly sanctions handing off to a prototype.
  The case and the skill disagree and one of them must change.

Case fix (2026-09-23, after this run): the `ungrillable` expectation was corrected, not loosened —
SKILL.md §7 sanctions a ROUTE with `target: prototype`, so `state_in` now includes ROUTE
(`not_target: implement` and the prototype regex are unchanged). The 0/1 above reflects the
mis-specified expectation, not a behaviour failure; see the comment in `evals/cases.yaml`.

## 5. Line for the README

```
2026-09-23 · opencode · dp/deepseek-flash · 8/10 cases · probe ratio 0.56 · 0 over-asks · 0 hallucinated evidence
```
