# Evaluation report — opencode

> 注：本报告为汇总重写版。全部 10 用例以 `--repeat 1` 运行（成本缩减方案，所有者批准）。
> `TIMEOUT` 由 240 提升至 480 后重跑了首轮超时的 3 个用例（原始 240s 超时转录保留在
> `raw/opencode/` 同名文件被覆盖前仅存于本轮日志；重跑结果为准）。

## 1. Environment

- date: 2026-09-22
- harness: opencode (1.18.32)
- model: opencode-go2/glm-5.3-flash（所有者指定；用户配置默认为 opencode-go/deepseek-v4.1-flash，
  经 `--model` 覆盖。渠道 opencode zen `https://opencode.ai/zen/go/v1`）
- skill commit: 7bd5aa9（SKILL.md 已含 claude-code 侧全部 9 处迭代修复）
- repeats per case: 1
- contamination check: **NO — preflight 判定假阳性**（见下）
- skill visible in a prepared workspace: yes（run.py 提取逻辑修复后，冒烟与首批用例均正常产出 spec）

### contamination 预检假阳性说明

`contamination_reply` 显示 runner 取到了**整个 JSONL 信封首行**（`{"type":"step_start",...}`）
而非模型回复文本——`harness_texts` 曾把 raw stdout 按长度排序取最长，JSONL 信封整体是最长
字符串。该 bug 已在 run.py 修复（保留事件顺序 + raw 仅作兜底）。修复后人工验证：干净目录
"Reply with exactly: OK" → 回复恰好 `OK`，**无用户级指令泄入的实质证据**。残余风险：opencode
无法像 claude-code 那样用 `--setting-sources` 隔离用户级配置，其全局 AGENTS.md/规则文件
（若存在）会进入上下文；本轮观测到的回复均为英文且未发现工作流指令渗出的行为痕迹。

### run.py 修复与偏差（opencode 特有）

1. `harness_texts` 保留事件顺序、raw stdout 信封不再参与 extract_spec 扫描：JSONL 场景下
   某事件打开的 ```yaml 围栏在后续事件才闭合，非贪婪正则会横跨多个事件拼出垃圾 blob，
   按"最长优先"它必胜出 → 整个用例被判 degraded_output。这是冒烟阶段 3 次连续
   degraded_output 的根因，已修。
2. `extract_spec` 改为从候选**末尾**扫描（最后含围栏的事件 = 最终答案）：原"最长优先"使
   SKILL.md 文档自身的示例围栏（skill 工具输出回显，20KB）盖过模型 4KB 的真实输出，
   runner 曾把文档示例当成模型产出断言。
3. `TIMEOUT` 240 → 480：glm-5.3-flash 在 no-repo / git-only-fact / chinese-ambiguous 上
   真实推理时间超过 240s（exit 124 timeout after 240s），提升后全部正常完成。

`opencode.json` written into every workspace:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "skill": "allow",
    "bash": {
      "git log*": "allow",
      "git show*": "allow",
      "*": "deny"
    },
    "edit": "deny",
    "task": "deny",
    "question": "deny",
    "webfetch": "deny",
    "websearch": "deny",
    "external_directory": "deny",
    "doom_loop": "allow"
  }
}
```

## 2. Summary

| metric | value |
|---|---|
| cases | 10 |
| passed | 5 |
| probe ratio (mean) | 0.47 |
| over-asks | 0 |
| auto-trigger | fires when it should: 1/1 |
| degraded output | 2 |
| not triggered | 0 |
| hallucinated evidence | 4 |

Threshold: at least 8 of 10 cases pass **and** hallucinated evidence is 0. Result: **NOT MET**（5/10，且幻觉 4 条 > 0）。

## 3. Per case

| case | states | passed | failed assertions |
|---|---|---|---|
| `add-caching-auto` | ASK | 1/1 | — |
| `add-caching-explicit` | ASK | 1/1 | — |
| `add-caching-two-turn` | ROUTE | 1/1 | — |
| `fully-specified` | ROUTE | 0/1 | evidence_valid（inferred 约束 evidence 填推理句） |
| `vague-no-ask` | HALT | 1/1 | — |
| `no-repo` | HALT | 0/1 | evidence_valid（空仓库中 evidence 填 "." ×2）, state（HALT≠ASK）, probed_constraints_eq |
| `question-not-trigger` | (quiet) | 1/1 | — |
| `git-only-fact` | ASK | 0/1 | evidence_valid（evidence 填 `.git/COMMIT_EDITMSG:1`） |
| `chinese-ambiguous` | - | 0/1 | degraded_output（yaml 未引号标量） |
| `ungrillable` | - | 0/1 | degraded_output（yaml 未引号标量 `{ users: [...] }`） |

失败模式归类（证据在 `raw/opencode/`）：
- **evidence 指针纪律**（3 例）：glm-5.3-flash 把推理句（"delegated by symmetry…"）、`.`、
  `.git/COMMIT_EDITMSG:1` 填进 evidence。前两类违反 SKILL.md §2 的指针定义（该节已修复但
  该模型仍违反）；`.git/` 内部文件虽在运行时工作区真实存在，但不符合 `git:<sha>` 形态——
  SKILL.md §2 未显式禁止 `.git/` 路径，**待修**。
- **yaml 输出纪律**（2 例 degraded）：未引号标量含冒号/花括号。与 claude-code 侧同根因，
  SKILL.md §6 缺 quoting 规则，**待修**。
- **no-repo 决策态**：空仓库该 ASK（问实现意图）却 HALT——模型把"无探测面"错误归为
  不可决而非可问。
- 正面结果：行为类用例（auto/explicit/two-turn/vague/question-not-trigger）5/5 通过，
  说明 SKILL.md 本轮 9 处迭代对"问什么、何时问"的修复在第二个模型上同样生效。

## 4. Iterations

- 本轮 opencode 侧未新增 SKILL.md 修改；所有迭代均产生于 claude-code 冒烟阶段并在本轮生效
  （详见 claude-code 报告 §4：unknown 键白名单、evidence 单指针、trace step 枚举、
  unknowns_found 重建计数、问序规则、语言硬规则、失效路径枚举、路由清单权威性、
  "排除选项≠解决未知"）。
- 生效证据：add-caching 系 3 例全部 ASK/ROUTE 正确、问题均为失效行为/兼容性偏好、
  resolution 计数守恒、无语言漂移（英文 prompt 全英文回复）。
- opencode 暴露的新缺陷（未修，交回所有者）：§2 未禁 `.git/` 内部路径作 evidence；
  §6 缺 yaml quoting 规则。

## 5. Line for the README

```
2026-09-22 · opencode · opencode-go2/glm-5.3-flash · 5/10 cases · probe ratio 0.47 · 0 over-asks · 4 hallucinated evidence
```
